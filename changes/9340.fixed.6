Fixed disabled rows on the Jobs list dimming the run link below the WCAG AA contrast minimum.
Fixed stacked links in the VLAN "Prefixes" and VRF route target table columns being too small to satisfy the WCAG 2.2 pointer target minimum.
Fixed the "Items per page" control on the theme preview page having no accessible name.
