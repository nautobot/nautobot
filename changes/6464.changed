The ImportObjects system job now rejects unrecognized fields/columns with a clear error instead of silently ignoring them, requires change permissions for updates (add permissions for creations), and updates matched existing records in place rather than failing on duplicates. CSV exports now include many-to-many fields and begin with a `# nautobot-import:` directive row carrying the model's match key.
