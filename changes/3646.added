Redirect unauthenticated users on either `/api/docs/` or `/graphql/` to login page if `HIDE_RESTRICTED_UI` is True.
Only time is shown on the footer if a user is unauthenticated and `HIDE_RESTRICTED_UI` is True.