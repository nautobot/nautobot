Added optional `lookup` parameter to `LinkedCountColumn`; if specified, the table can prefetch the first related object for this column, and if it's the only such object, display it directly instead of just displaying `1`.
