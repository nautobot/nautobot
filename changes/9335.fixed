Fixed OpenTelemetry log correlation duplicating and reformatting log lines when `OTEL_PYTHON_LOG_CORRELATION` was enabled, by injecting the trace and span IDs onto log records instead of overriding the root logging configuration.
