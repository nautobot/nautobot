Added JobQueue and JobQueueAssignment models and API.
Added a data migration to represent `Job.task_queues` with `JobQueue` instances.
Added a data migration to represent `ScheduledJob.queue` to with a `JobQueue` instance.
