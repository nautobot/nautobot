Added a new "Monitoring" section to the User Guide > Administration documentation, covering the three-collector model (logs, metrics, probes), recommended alert recipes, Celery and Job reliability tuning (including the Redis visibility-timeout pitfall), and Redis / PostgreSQL operational guidance. Moved the existing "Health Checks", "Prometheus Metrics", and "Request Profiling" pages from "Guides" into the new "Monitoring" section; redirects from the old paths are in place.
