Added `CELERY_WORKER_PROMETHEUS_PORTS` configuration setting
Added prometheus HTTP server listening on the worker to expose worker metrics
Added `nautobot_job_duration_seconds` counter metric that reports on job execution
