Added listing of related `files` to the `/api/extras/job-results/` REST API.
Added read-only REST API for the FileProxy model (files generated by a Job run), including a `/download/` endpoint for downloading the file content.
