Fixed issue where the documented environment variables for various configuration settings would not be supported if using a minimal `nautobot_config.py` that was not generated from `nautobot-server init`.
