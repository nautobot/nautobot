Added a Playwright-based UI test framework as the forward standard for browser-based test coverage: shared page-object and fixture infrastructure in `nautobot/playwright/`, per-app test suites under `nautobot/<app>/tests/integration/`, an optional `playwright` dependency group, an `invoke playwright` task, and a `playwright-test` CI job. The Locations list view filter Selenium test was replaced by its Playwright equivalent.
