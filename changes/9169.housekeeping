Extracted shared namespace and parent-Prefix resolution logic from `IPAddress` and `IPAddressRange` into a reusable `NamespaceParentedModelMixin`.
Improved performance of the prefix IP Addresses view for prefixes with many IP Address Ranges.
