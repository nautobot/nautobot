Natural key refinement for multiple DCIM models
1. Rearrange the natural-key fields for `PowerOutlet` and PowerPanel.
2. Change the natural-key fields for `Cable`, `CablePath`, and `TaggedItem` to merely "pk"
3. Change the default key for `RackReservation` to `["units", "racks"]`
4. Change the natural-key of `VirtualChassis` to "name"
