Added `Canceled` Approval Workflow State.
Added `PullRequest` to Approval Workflow Models.
Approval Workflows and their various sub-models were flagged as non-versionable.
Added `approval_workflows` to `EXTRAS_FEATURES` constants.
