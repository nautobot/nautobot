Added `pre_delete` signal to catch invalid operations to remove device's `primary_ip` from interface's `ip_addresses` m2m field.
Added validations to `InterfaceForm` and `VMInterfaceForm` `clean()` methods to catch invalid operations to remove device's `primary_ip` from interface's `ip_addresses` m2m field.
Added `pre_save` signal to catch invalid operations in manual creations of `IPAddressToInterface`.
Added `unique_together` for through model `IPAddressToInterface`.
