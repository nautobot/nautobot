Added a "Searchable Fields by Model" documentation page, generated from each model's `SearchFilter` via the new `generate_searchable_fields` management command, listing the fields matched by each model's list-view search bar.
