Fixed the `GET` revoke API behavior for JobResult objects already in a terminal state. The endpoint now returns `200 OK` with a `None` preview instead of presenting a `REAP` or `TERMINATE` action. `POST` requests against completed jobs continue to return `409 Conflict`.
