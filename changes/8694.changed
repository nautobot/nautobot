Added an `is_staff_only_changelog_model` model attribute. When `True`, change-log entries for that model are hidden from non-staff users. Set to `True` by default on `User`, `Group`, `Token`, and `ObjectPermission`, all of which now record change-log entries; the new attribute keeps those entries restricted to staff users.
