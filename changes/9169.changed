Improved performance of `IPAddress` and `IPAddressRange` validation by memoizing closest-parent Prefix lookups per namespace and host, avoiding redundant database queries between `clean()` and `save()`.
