Playwright browser contexts now send the `X-Disable-Debug-Toolbar` header, and the development configuration keeps django-debug-toolbar off pages that carry it or when `NAUTOBOT_SHOW_DJDT_TOOLBAR` is set to a falsy value.
