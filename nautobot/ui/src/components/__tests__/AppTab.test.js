describe("create_app_tab", () => {
    it("should render a tab with correct content", async () => {
        // const mockTab = { title: 'Test Tab', url: 'http://example.com/test' };
        // const mockHTML = '<div id="legacy-content">Test HTML Tab</div>';
        // // Spy on the global fetch function and provide a custom implementation to simulate a response
        // jest.spyOn(global, 'fetch').mockImplementation(() =>
        //   Promise.resolve({
        //     text: () => Promise.resolve(mockHTML),
        //   })
        // );
        // let container;
        // await act(async () => {
        //   const renderResult = render(<Tabs>{create_app_tab({ tab: mockTab })}</Tabs>);
        //   container = renderResult.container;
        // });
        // await screen.findByText('Test HTML Tab');
        // const innerHTML = container.querySelector('.nav-link.active').innerHTML;
        // expect(innerHTML).toEqual('Test Tab');
        // global.fetch.mockRestore();
    });
});
