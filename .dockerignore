# Exclude everything by default
*

# Un-exclude specific things needed for the Docker build
!development/nautobot_config.py
!development/dolt-config.yaml
!docker/docker-entrypoint.sh
!docker/uwsgi.ini
!examples
!nautobot
!poetry.lock
!pyproject.toml
!README.md
!mkdocs.yml
!package.json
!package-lock.json

# Add specific exclusions
**/__pycache__
# Installed in the image by `npm ci`; including the host's copy only bloats the context
**/node_modules
