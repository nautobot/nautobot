---
version: "3.4"
services:
  nautobot:
    depends_on:
      - postgres
  postgres:
    image: postgres:13
    env_file:
      - dev.env
    volumes:
      - pgdata_nautobot:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  pgdata_nautobot:
