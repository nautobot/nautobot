# Used to run the final container images vs the dev containers
---
version: "3"
services:
  nautobot:
    build:
      target: final
    image: "networktocode/nautobot:local"
    ports:
      - 8443:8443
  worker:
    image: "networktocode/nautobot:local"
