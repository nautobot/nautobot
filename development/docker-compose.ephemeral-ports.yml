# Set all ports to ephemeral to avoid conflicts with other services.
---
services:
  nautobot:
    ports: !override
      - "8080"
      - "6899"
  celery_worker:
    ports: !override
      - "8080"
      - "6898"
  mkdocs:
    ports: !override
      - "8001"
  selenium:
    ports: !override
        - "4444"
        - "5900"
