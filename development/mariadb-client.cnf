[client]
# In Debian 13 "Trixie" and later this defaults to TRUE, which would require us copying the self-signed certs autogenerated
# by the mysql docker container and setting them as trusted.
# Since this is for our development environment only, easier to just disable this setting locally.
ssl-verify-server-cert=FALSE
